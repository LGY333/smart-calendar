# 网页端一个月计划

范围：只负责网页端产品，后端和模型由其他同学负责。

## 第 1 周：把前端和 AI 解耦

目标：后端还没好也能继续开发，后端好了能快速接入。

任务：
- [ ] 增加 `api` 抽象层，统一管理所有后端请求
- [ ] 增加 `mock` 适配器，模拟后端返回结果
- [ ] AI 输入、自动排程、目标规划都改成调用 `api`
- [ ] 没有后端时自动使用 `mock`，不影响演示
- [ ] 与后端同学确认接口格式

验收：关闭 mock、填上真实接口地址后，前端不需要改业务逻辑。

## 第 2 周：产品体验打磨

目标：让网页看起来是完整产品，不是功能堆叠。

任务：
- [ ] 统一空状态、加载状态、错误状态
- [ ] 移动端底部导航和信息层级优化
- [ ] 暗色模式、对比度、键盘操作、屏幕阅读器
- [ ] 月/日/周/议程切换动画
- [ ] 任务卡片、目标看板、助理页视觉统一
- [ ] 任务拖拽、拉伸、跨日移动的移动端体验

验收：手机和桌面端都能顺畅完成核心流程。

## 第 3 周：语音、通知与离线

目标：把网页端特色功能补齐。

任务：
- [ ] 完善语音通话 UI 和状态提示
- [ ] 语音识别结果交给后端，语音回复由前端播放
- [ ] 浏览器通知与提醒
- [ ] PWA 安装和离线缓存
- [ ] JSON/ICS 导入导出

验收：允许麦克风后能语音对话；网络断开时仍能打开日历。

## 第 4 周：演示和验收

目标：现场演示不翻车。

任务：
- [ ] 准备 3 分钟演示脚本
- [ ] 准备手机演示路径
- [ ] 处理边界情况：无任务、无目标、解析失败、接口超时
- [ ] 统一按钮、弹窗、提示文案
- [ ] 用 Edge 或 Chrome 做完整流程回归

验收：从打开页面到完成核心闭环，全程不需要解释也能看懂。

## 当前重点

本周先做第一周：建立前端 `api` 抽象层和 `mock` 数据层，让后端未完成时也能继续独立开发。
